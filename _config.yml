title: "Oleks’ Lair"
description: "Dev, drones, dirt bikes, and dark humor"
remote_theme: pages-themes/hacker@v0.2.0
plugins:
  - jekyll-remote-theme
markdown: kramdown
highlighter: rouge


# Where your posts live
collections:
  posts:
    output: true

# Simple navigation menu (Hacker theme doesn’t auto-render, so you’ll use links in your pages/layouts)
nav:
  - name: "Home"
    link: "/"
  - name: "About"
    link: "/about/"
  - name: "Projects"
    link: "/projects/"

# Optional tweaks
markdown: kramdown
highlighter: rouge
