name = "olekssobol-portfolio"
compatibility_date = "2024-01-01"

pages_build_output_dir = "_site"

[site]
bucket = "_site"

# Build configuration
# Cloudflare Pages will automatically detect Jekyll
# but you can override build settings if needed
